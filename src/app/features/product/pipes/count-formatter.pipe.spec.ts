import { CountFormatterPipe } from './count-formatter.pipe';

describe('CountFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
