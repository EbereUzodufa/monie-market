import { ShippingArrivalPipe } from './shipping-arrival.pipe';

describe('ShippingArrivalPipe', () => {
  it('create an instance', () => {
    const pipe = new ShippingArrivalPipe();
    expect(pipe).toBeTruthy();
  });
});
