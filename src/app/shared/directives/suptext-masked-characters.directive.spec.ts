import { SuptextMaskedCharactersDirective } from './suptext-masked-characters.directive';

describe('SuptextMaskedCharactersDirective', () => {
  it('should create an instance', () => {
    const directive = new SuptextMaskedCharactersDirective();
    expect(directive).toBeTruthy();
  });
});
